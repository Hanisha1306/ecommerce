# Getting Started with Create React App

This project was bootstrapped with [Create React App](https://github.com/facebook/create-react-app).

## Available Scripts

In the project directory, you can run:

### `npm start`

Runs the app in the development mode.\
Open [http://localhost:3000](http://localhost:3000) to view it in your browser.

The page will reload when you make changes.\
You may also see any lint errors in the console.

# project Name - MY-APP
# Technologies Used

    React.js (v19.2.0)
    React-Router-DOM
    Bootstrap 5
    JavaScript (ES6)
    DummyJSON API

 # The project is created by HanishaCheekati 
 
 # project Details 
    1.Created the project using npx create-react-app.
    2.Set up  folder structure with components and containers directories.
    3.Bootstrap by adding the CDN link to the index.html file for responsive and modern styling.
    4.Used Bootstrap card components for product UI.
    5.Created Product Card Designed reusable product cards based on Bootstrap documentation.   
    Each card displays the product image, title, price, and a button to view details.
    6. React Router to navigate between pages:
    Home Page – Displays all products.
    Description Page – Shows detailed information about a selected product using useParams.
    Categories Page – Displays products filtered by category.
    7 Added a sidebar to navigate between Home and Categories.
    8 Implemented a search bar to filter products dynamically based on user input with the help of  api .
    9 Used useParams to fetch and display selected product details and implemented navigation from product cards to individual product pages.
    10 fetched categories using Displayed categories as buttons.
 
 # About Project 
  This project is a React-based product listing app that displays, searches, and filters products by category using the DummyJSON API with Bootstrap styling and React Router navigation.